# WuWa Cubie Derby — 开发进程

## 2026-05-10 规则梳理与确认

### 已完成

- [x] 阅读原始规则文档
- [x] 提出第一轮疑问（10项），覆盖装置方向、能力结算顺序、堆叠机制、相遇定义等
- [x] 文档更新后重新阅读，提出第二轮疑问（3项）
- [x] 获得全部澄清，关键确认如下：
  - 装置对X和普通选手绝对方向相同（前进+1，后退-1），"相反"仅指战略效果
  - 装置允许连续触发
  - **所有路过都进行堆叠操作**（包括普通选手），物理模型为"踩在头上"
  - 先堆叠再触发装置
  - 装置作用于移动发起者（底层选手）
  - 被携带到装置不触发陆的技能
  - 西的标记在回合开始时确定，移动途中事件与西无关
  - 娅比较基础骰而非最终结果
  - 绯与X相遇：只要同格过就算，包括被携带场景
  - 绯的+1从下一轮生效
  - 多人同时到终点：排名最高者获胜
  - 扰乱装置重排后X仍强制底层
  - 被携带时不触发技能
- [x] 更新规则文档为完整版

---

## 2026-05-10 核心实现

### 已完成

- [x] 项目结构创建：src/abilities/, tests/
- [x] track.py：赛道模型、装置定义（DEVICE_MAP）、DeviceType枚举
- [x] player.py：PlayerState数据类，含prev_base_dice追踪
- [x] ranking.py：compute_ranking、get_rank函数
- [x] abilities/：6个选手能力模块（陆/西/娅/绯/卡/菲），全部实现
- [x] game.py：完整游戏引擎
  - 回合流程：生成移动顺序→掷骰→结算→依次移动→回合结束效果
  - 连续单格移动 + 路过堆叠 + 装置触发（含连续触发）
  - X反向移动 + 始终底层 + 传送机制
  - 绯-X相遇检测（全场景覆盖）
  - 胜负判定（含多人同时到终点排名）
- [x] simulator.py：Monte Carlo模拟器
- [x] main.py：入口程序

### Bug修复

- [x] 娅能力bug：last_base_dice被覆盖，改用prev_base_dice追踪上轮基础骰
- [x] 西能力bug：_marked在每次on_round_start调用时重置，改为仅在西自身调用时重置
- [x] 首轮顺序bug：首轮移动顺序应与起始堆叠顺序一致
- [x] on_round_start调用方式：改为每个能力每轮只调用一次

### 测试验证

- [x] 19项测试全部通过（test_simulation.py 10项 + test_detailed.py 9项）
  - 胜者合法性、位置有效性、X始终底层、堆叠无重复
  - 位置与堆叠一致性、排名无并列、娅能力正确性
  - 绯相遇X、X传送、胜率总和为1
  - 装置位置、骰子范围、prev_base_dice追踪、排名顺序
  - 扰乱装置后X底层、绯加成生效、卡能力末位激活后永久生效
  - 首轮顺序匹配、回合数合理

---

## 2026-05-10 性能优化

### 优化内容

- [x] 消除`_game_ctx`热路径：不再每次调用创建新dict，改为局部变量直接传递
- [x] `__slots__`优化Game类：减少属性访问开销
- [x] 缓存`_fei`/`_x`引用：避免重复dict查找
- [x] `NORMAL_NAMES`改为tuple：不可变，微优化
- [x] `_move_group_to_finish`专用路径：终点移动无需X底层判断
- [x] 提前返回优化：`_apply_x_teleport`中`x_pos >= last_place_pos`提前退出
- [x] multiprocessing并行：`Simulator`支持`workers`参数，按worker分片+独立seed
- [x] main.py升级：支持`-n`/`-w`参数，默认1M局，显示耗时和吞吐量

### 性能对比

| 配置 | 局数 | 耗时 | 吞吐量 |
|------|------|------|--------|
| 优化前 单线程 | 100K | 54s | 1,850 games/s |
| 优化后 单线程 | 100K | 38s | 2,650 games/s |
| 优化后 32核 | 1M | 12s | 84,000 games/s |
| 优化后 64核 | 10M | 65s | 153,000 games/s |
| 优化后 120核 | 100M | 565s | 177,000 games/s |

---

## 2026-05-10 代码审计与Bug修复

### 西标记逻辑Bug

- **问题**：西的标记逻辑遍历排名从第1名开始，始终标记排名最高的2名选手，而非"紧邻西之上的最多2名选手"
- **规则**："标记排名高于且紧邻西的最多2名选手"（西排名4→标记第3、第2名）
- **原因**：`for name in ranking: if r < xi_rank` 从rank 1开始遍历，前两个满足条件的始终是rank 1和rank 2
- **修复**：改为直接取`ranking[xi_idx - 1]`和`ranking[xi_idx - 2]`（即紧邻西上方的2名选手）
- **影响**：修复前西始终debuff前2名（通常是娅和绯），人为压缩胜率差距

### 全量审计结果

对6个能力模块、游戏引擎、堆叠、排名、X行为、胜负判定逐一对照规则文档审计，**除西标记bug外无其他问题**。

### 10M局结果

| 选手 | 胜场 | 胜率 |
|------|------|------|
| 陆 | 1,459,325 | 14.59% |
| 西 | 1,554,992 | 15.55% |
| 娅 | 2,085,262 | 20.85% |
| 绯 | 1,717,695 | 17.18% |
| 卡 | 1,467,037 | 14.67% |
| 菲 | 1,715,689 | 17.16% |

### 用法

```bash
python3 -m src.main                    # 默认1M局，自动检测核数
python3 -m src.main -n 10000000        # 10M局
python3 -m src.main -n 100000000 -w 64 # 100M局，64进程
```
